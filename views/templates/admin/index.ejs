<%- include("../../partials/admin/head")%>
<body>
    <%- include("../../partials/admin/navbar")%>
    <!-- End of Header -->
    <h2 class="all-course-heading">All Courses</h2>

    <!-- Content blocks -->
    <div class="main_blocks">
        <% for(var crs of allCourses) {%>
        <div class="card mb-3">
            <div class="col-sm-6">
                <div class="col-md-4 img-class">
                    <img src="<%=crs.image%>" class="img-fluid rounded-start" alt="...">
                </div>
                <div class="col-md">
                    <div class="card-body">
                        <h4><%=crs.title%></h4>
                        <p class="platform"><%=crs.platform%></p>
                        <p class="type tag"><%=crs.price%>, <%=crs.type%></p>
                        <a href="/admin/courses/<%=crs._id%>">View details</a>
                    </div>
                </div>
            </div>
        </div>
        <%} %>
    </div>
    </div> 
    <!-- End of content block -->
    <%- include("../../partials/admin/footer")%>

    <script src="/js/script.js"></script>
</body>

</html>