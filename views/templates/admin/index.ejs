<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Resources</title>

    <!-- Boxicon link -->
    <link
      href="https://unpkg.com/boxicons@2.1.2/css/boxicons.min.css"
      rel="stylesheet"
    />

    <!-- Bootsrap -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3"
      crossorigin="anonymous"
    />

    <!-- CSS link -->
    <link rel="stylesheet" href="/css/resources.css" />

    <!-- Favicon icon link -->
    <link rel="icon" type="image/png" href="/icons/title.png" />

    <!-- Google font link-->
    <link
      href="https://fonts.googleapis.com/css2?family=Poppins:wght@200;500;600&display=swap"
      rel="stylesheet"
    />
  </head>
  <body>
    <header>
      <!-- Navbar -->
      <div class="navigation">
        <div class="navbar_logo">
          <a href="index.html"
            ><img src="/icons/LS_logo.png" class="icon1"
          /></a>
        </div>
        <div class="navbar_links">
          <a href="/admin/courses/add" class="nav_link">Add Course</a>
          <form action="/admin/logout" method="POST">
            <button>Logout</button>
          </form>
        </div>
      </div>
    </header>
    <!-- End of header -->

    <h2>All courses</h2>
    <p class="text-style"><%= success %></p>

    <div class="container">
      <% for(let crs of courses) {%>
      <div class="card mb-3">
        <div class="col-sm-6">
          <div class="col-md-4">
            <img
              src="<%= crs.image%>"
              class="img-fluid rounded-start"
              alt="..."
            />
          </div>
          <div class="col-md-8">
            <div class="card-body">
              <p class="card_title"><b><%=crs.title%></b></p>
              <div class="desc">
                <span class="card_text"><%=crs.platform%>, </span>
                <span class="card_text"><%=crs.price%></span>
              </div>
              <div>
                <a href="/admin/courses/<%=crs._id%>">View more</a
                ><a href="<%=crs.url%>" target="_blank">Go to course</a>
              </div>
            </div>
          </div>
        </div>
      </div>
      <%} %>
    </div>

    <script src="/js/flashAlert.js"></script>
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p"
      crossorigin="anonymous"
    ></script>
  </body>
</html>
